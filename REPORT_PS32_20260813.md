# ps32_mode K=32 部分和可行性调查报告（路径 A 硬件判负）

日期：2026-08-13
调查：TPU 底层工程师（12 设备探针，CV1800B 实测）
联动：推理引擎工程师（per-chunk 无 ps32 宿主扫参）
记录：DUO project CEO
状态：调查完成 → 路径 A（硬件 per-group dequant matmul）正式判负

---

## 0. 结论（TL;DR）

bmk1822 ps32_mode 按 K=32 切片出 int32 部分和 → 乘 fp32 scale → 跨组累加的路径，
在当前 CV1800B SDK 下**不可用**。生产形状 N=896/4864 被三条硬性证据否决。

**替代出口：per-chunk INT8 matmul（KG≤256）+ 自适应 per-row rshift + CPU fp32 累加，
ps32-free，宿主仿真 3/3 全过。**

---

## 1. 判定：不可行（三条硬性证据）

1. **无 TIU 链式累加**：库内 `assert(!p->add_result)` 硬断言，matmul 禁止 add_result=1，
   无法在 TIU 内跨组累加部分和，只能导出后 CPU 处理。
2. **ps32 分配器缺陷**：`lmem_alloc_ps32_matrix` / `lmem_ps32_matrix_to_size` 对 [1,896]
   只预留 448B（marker 实测），而 ps32 输出需 ~3584B+ → lmem 溢出。
3. **列丢失**：ps32 缓冲只导出 w 列（N=112/224/448/896 → w=16/32/64/112，约 N/7~N/8），
   其余列静默丢弃；FMT_I32 matrix l2g 不能去交织布局，读回全错。

**性能否决**：即便绕过上述限制，292 切片/层 × 24 层 = 7008 submits/token ≈ 1.4s/token
增加，超过当前整个 decode 时间。

---

## 2. 验证方式（可复现）

- 12 个 `ps32_probe*.c` 设备探针（保留在 `tpu_bench/`，本次提交入库），CV1800B 上板实测。
- identity 技巧（全列=1/-1）反推布局 + 随机值逐字节比对 host 仿真。
- N=112/224/448/896 系列扫描确立列宽规律。
- 小 N（≤112）ps32_mode=2 + res_is_int8=1 可导出真 int32 部分和（逐字节精确）→ 能力存在，
  但被生产形状的三条硬性证据否决。

---

## 3. 关键联动发现：ps32-free 出口（质量 3/3）

推理引擎工程师的宿主扫参（`experiments/qwen_onboard/chunk_sweep.json`）：
**per-chunk INT8 matmul（Path B 直接 per-channel INT8 权重）+ 自适应 per-row rshift +
CPU fp32 累加**，KG=32/64/128/256 全部 3/3 通过，且不用 ps32。

| KG | 3 prompt next_token | 判定 |
|---|---|---|
| 32  | 全中（gap 0.52–1.33） | 3/3 ✓ |
| 64  | 全中（gap 0.005–1.09） | 3/3 ✓ |
| 128 | 全中（gap 0.22–1.23） | 3/3 ✓ |
| 256 | 全中（gap 0.19–0.89） | 3/3 ✓ |

→ 硬件 submit 次数随 ~K/KG 下降，KG=256 为最低成本可行点（需 TPU 上板实测确认）。

---

## 4. 对项目影响与裁决

1. **路径 A（INT4 K-aligned per-group dequant matmul via ps32）正式判负**，与
   DESIGN_INT4_WEIGHTS.md 的 FLAT 布局结论一致。
2. **INT4 存储路径关闭**：硬件（ps32）不可行 + CPU 解包（weight-side dequant）质量 0/3 判负。
3. **Qwen 采用 Path B（per-channel INT8）**，通过 per-chunk 自适应 rshift matmul + CPU fp32
   累加实现质量 3/3，ps32-free、TPU 可行。
4. **SmolLM2 INT8 生产路径不受影响**（基线 next_token=5021 已验证）。

---

## 5. 下一步

1. 推理引擎工程师：收敛 A/B 工期对比（含 per-chunk KG=256 submit 开销）、ION 24MB 缓冲方案、
   正式 REPORT。
2. TPU 底层工程师：上板验证 per-chunk INT8 matmul（KG=256 自适应 rshift + CPU fp32 累加）
   可实现性与实测 submit 延迟。

---

## 6. 裁决更新（2026-08-13 傍晚）：ps32-free 两遍法 → Path A 复活为推荐路径

**新决定性发现（推理引擎工程师，host 验证，数据核验通过）**：ps32 不是必需的。
per-chunk K 切分 matmul + **两遍 rshift**（pass1 安全 rshift 读 int8 输出 → CPU 算
per-chunk 真实 max → pass2 精化 rshift 满幅输出）→ CPU fp32 累加，
**Path A（INT4 G32 K-aligned）与 Path B（per-ch INT8）均达 3/3**。

| 配置 | next_token（3 prompt） | gap 余量 |
|---|---|---|
| Path A INT4 G32 K-aligned + 两遍法 | 3/3 ✓ | **1.923 / 1.105 / 0.485（最强）** |
| Path B per-ch INT8 + 两遍法（KG=128） | 3/3 ✓ | 1.129 / 0.704 / 0.676 |
| Path B + per-row act + 全 K 单 rshift（旧） | 1/3 ✗ | — |
| bound-based rshift（无读回） | 1–2/3 ✗ | — |

关键机理：输出 int8 rounding 是 razor-thin logits 的根本瓶颈（hilo_exact=3/3 vs
hilo_rs=0/3）；per-chunk 切分 + **真实 max 的 rshift** 是解药；确定性上界过粗，
必须两遍读回。Path B 取 KG=128 稳健（KG=256 退化 2/3）；Path A 用 KG=32=G。

**裁决（§4 更新）**：§4.3 原「Qwen 采用 Path B」**更新为——
Qwen 采用 Path A（INT4 G32 K-aligned + 两遍法）**。理由：SD 带宽主导瓶颈，Path A 带宽
少 1.78x（201 vs 358MB/token）、ION 双缓冲 16.8MB FIT 24MB、gap 余量最大。工作量 3–4 人日
（操作者已接受 ~6 人日上限），无 ps32 风险。

**待确认闸口（TPU 底层工程师上板验证）**：
① pass1 读 int8 输出 = 标准 matrix_multiplication 语义，可直接回读（开工闸口）；
② Path A KG=32 → ~640 submits/layer（K=896→28 块 ×6 + down K=4864→152 块，×2 遍）的
   延迟是否可被 SD 读（201MB/token @21.9MB/s ≈ 9.2s）流水线掩盖；
③ ION 24MB 内 CPU per-group dequant（INT4→INT8）工作缓冲布局。

---

## 7. 最终裁定（2026-08-13 晚，TPU 上板实证 + 双工程师共识）

TPU 底层工程师上板裁定（`PS32_PER_GROUP_VERDICT_20260813.md`，probe18-21）：

**ps32 硬件路径可行，但被两遍法支配**：
- ps32 int32 部分和可读（byte-plane 布局，N=192/次为 TIU 硬件上限，forced-c1 全 N 列可覆盖）；
- fp32 输出不可读（FMT_BF16 全零）→ 必须 CPU fp32 累加；
- 批量成本 ~1.9μs build + 1.8μs run/op，cmdbuf ~2000 op/256KB → 提交量非瓶颈；
- submit ~58k/token（层内）+ ~22k（lm_head）≈ 80k，**比两遍法 49.5k 还多**；
- 工作量 7-12 人日（预算 3-4x），无质量增益（两遍法已 3/3）。

**主路径裁定：Path A 两遍法（ps32-free）**；ps32 路径保留为文档化 fallback
（若两遍法上板 razor-thin prompt 余量不足，ps32 精确部分和可兜底）。

**decode 上界修正（重要）**：两种路径的 TIU/计算均 ~200-500ms/token，但 **decode 为 SD
受限**——权重 201MB + LM head 136MB ≈ 11.6s/token（29MB/s）。计算路径选择（两遍法 vs ps32）
**不影响 decode 上界**；真正的解码杠杆在 Phase 6（LM head top-k / 权重驻留 / 带宽）。

**闸口状态**：②（提交量）已关闭（probe21 批量成本）；①④（pass1 int8 回读、per-call
rshift、N-tile=512）在 Path A 实现时随上板验证（标准 INT8 matmul，风险低）。
