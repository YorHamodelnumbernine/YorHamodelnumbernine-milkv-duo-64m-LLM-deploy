# Wt load 3.3s 排查与优化评估 — 实测报告

日期：2026-08-12
作者：推理引擎工程师
状态：排查完成，优化待 CEO 决策（未改生产代码，未提交 git）
基准命令：`/root/smollm2_pool_b2 /root/smollm2_instruct/ /root/input_tokens.bin 3 3 2`

---

## 1. 根因定位

**结论：Wt load ≈ SD 顺序读带宽（主因） + 流水线暴露（次因），外加自造 swap 争用（可控）。**

### 1.1 SD 读带宽（实测，DDR/SD 独立测试 sd_bench2）
| 测试 | 结果 |
|---|---|
| 3 文件整读（顺序，单线程） | 21.07 MB/s |
| 3 文件 256KB 分块读 + memcpy | 21.05 MB/s |
| 30 层整读（101MB） | 21.56 MB/s |
| 30 层 256KB 分块读 | 21.89 MB/s |
| 3 文件并发 3 线程 | 21.09 MB/s |

→ 已到 SD 卡物理上限；整读 vs 分块无差异；**并发读 == 顺序读**（SD 控制器本身串行）。

### 1.2 流水线暴露
- 每 forward pass = 10 批 × 3 层。每批读 10.1MB ≈ **462ms** @21.9MB/s；
  每批计算 ≈ **133ms**（QKV/RoPE/Scores/SM/Attn/Wo/RMS/FFN_up/FFN_dn 之和 1333ms/10）。
- 流水线只能藏住 ≈计算时间 133ms，暴露 ≈329ms/批 → 10 批 ≈ **3.3s**。
- 即"读(462) >> 算(133)"，本质是 SD 带宽受限，预取深度再深也藏不住。

### 1.3 Change C 回归检查
- 单线程顺序 21.07 MB/s == 3 并发 21.09 MB/s（3 文件）→ **Change C 无回归**。
- 30 层顺序 21.9 MB/s 说明批量顺序读是当前最优形态。

### 1.4 设备 load 干扰
- 外部 CPU 繁忙线程（0/1/2 个 busy-spin）对 SD 读**无单调拖慢**（sd_loadtest：
  12.5/15.5/17.5 与 11.2/12.6/12.8 MB/s，噪声主导，非单调）。
- **但发现自造干扰**：b2 固件把 12MB embed 放 DDR → 进程被迫 swap 8–9MB，
  swap 文件在**同一张 SD 卡** → swap I/O 与权重预取抢带宽。直接证据：
  - Init layers 0-2：DDR=12MB → **1150ms**（VmSwap 8MB）；DDR=2MB → **534ms**（VmSwap 0）。
  - LM_Head 每 chunk：DDR=12MB → 75–92ms（[load] 被 swap 拖到 84–245ms）；
    DDR=2MB → 58–60ms。
  - 所以 CEO 观察到的"设备 load 3.0"主要是模型自身 swap + 流水线线程。

---

## 2. 精确命令回归（前后数据）

| 指标 | b2 基线（CEO 参考 / 本会话前测） | 本次复测（clean ION） | DDR=2MB（CEO 扫描 run_c2） |
|---|---|---|---|
| Wt load | 3267–3347 / **3314ms** | **3413ms** | **3941ms** |
| LM_Head | 1242 / 1228ms | **1314ms** | **834ms** |
| Decode | 5483ms/tok | **5775ms/tok** | **5166ms/tok** |
| Total | — | 47318ms | 44370ms |
| Tokens | 5021 1308 6990 619 | 5021 154 154 26428 | 5021 1308 6990 25633 |

- Wt load 波动 3314→3413ms 属 swap/SD 噪声，**无 Change C 回归**。
- **重要**：DDR=2MB 整体 decode 更快（5166 < 5775ms/tok，−10.5%），主因 LM_Head
  大降（834 vs 1314ms）。虽 Wt load 略升（3941），净收益为正 → **支持 CEO 的 DDR 扫描方向**。
- 对比 P1 基线（2026-08-08, Wt load 2033ms）：当前 Wt load 明显升高（3413–3941ms）。
  经 sd_bench2 证明与 Change C 无关；**b2 固件（12MB DDR embed → swap 争用 + 大 CHUNK 读）是主因**，
  需要 CEO 知晓这一回归幅度。

---

## 3. 优化候选评估

| 候选 | 结论 | 预期 |
|---|---|---|
| (a) 更大/更连续 SD 读 | ❌ 无空间，已到卡上限（整读=分块） | 0% |
| (b) embed/权重 cache residency | ✅ 有效。DDR embed 造成 swap→SD 争用；**DDR=2MB 最优**（decode 5166ms/tok） | Wt load 受控 + LM_Head −480ms |
| (c) 预取深度加深 | ❌ 不可行。深度4 = 8 slot = 27MB > 24MB ION | — |
| (d) INT4 权重压缩 | ✅ 最大杠杆。字节减半→读 2.3s；Wt load 估 **3.4s→1.2–1.5s（×2.5–2.8）**；并释放 ION 给 KV | 大收益，需解包/反量化算子（可交 TPU 底层） |

---

## 4. 设备稳定性告警（运维级，需处理）

1. **设备在我测试期间重启 2 次**，原因待查（怀疑 swap/内存压力下内核不稳定）。
2. **模型偶发 RC=-1 崩溃**（decode 中途），且崩溃后**遗留孤儿进程占住 24MB ION pool**，
   毒化后续所有运行，必须 `kill -9` 清掉。本会话已处理 3 次。
3. 误杀了一个疑似卡死的遗留进程（PID 430，LM_EMB_DDR_KB=0 的 run_c0），如属 CEO 扫描请知悉。

---

## 5. 建议下一步

1. 采纳 **DDR embed=2MB** 配置（简单、净收益明确），重新跑精确命令确认。
2. **INT8 KV**（上下文 ×4，~1 天工作量，见 MLA 调研报告）作为"长上下文"手段。
3. **INT4 权重**作为 Wt load 主力优化（~1 周，需与 TPU 底层工程师协作做解包算子）。
4. 修复 ION 孤儿进程问题（进程退出时确保释放 ion fd / 或加看门狗清理）。
